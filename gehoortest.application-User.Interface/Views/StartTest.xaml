<UserControl x:Class="gehoortest.application_User.Interface.Views.StartTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:gehoortest.application_User.Interface.Views" 
        xmlns:viewmodels="clr-namespace:gehoortest.application_User.Interface.ViewModels" 
        mc:Ignorable="d"
        Height="650" Width="1450">
    <Grid>
        <local:TestExplaination Visibility="{Binding IntroText}" />
        <local:Question Visibility="{Binding QuestionText}" />
        <local:AudiometryQuestion Visibility="{Binding QuestionAudio}" />
    </Grid>
</UserControl>
